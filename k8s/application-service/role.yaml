apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: application-service-role
  namespace: default

rules:
  - apiGroups:
      - ""
    resources:
      - configmaps
    resourceNames:
      - application-service-config
    verbs:
      - get