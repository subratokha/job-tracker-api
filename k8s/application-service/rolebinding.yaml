apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: application-service-rolebinding
  namespace: default

subjects:
  - kind: ServiceAccount
    name: application-service-account
    namespace: default

roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: application-service-role