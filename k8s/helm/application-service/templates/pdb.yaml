apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: "{{ .Release.Name }}-application-service-pdb"
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: application-service
      app.kubernetes.io/instance: "{{ .Release.Name }}"