apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: "{{ .Release.Name }}-application-service-role"

rules:
  - apiGroups:
      - ""
    resources:
      - configmaps
    resourceNames:
      - "{{ .Release.Name }}-application-service-config"
    verbs:
      - get